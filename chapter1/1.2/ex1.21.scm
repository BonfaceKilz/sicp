;; Exercise 1.21:
;; Finding the smallest divisor in each of the following numbers

(load "primality_test.scm")

(display (smallest-divisor 199))
(newline)
(display (smallest-divisor 1999))
(newline)
(display (smallest-divisor 19999))
